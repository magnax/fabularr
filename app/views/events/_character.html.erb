<div>
  <% if @character != character %>
    <%= link_to t('character.actions.talk'), character_talk_url(character.id) %>
  <% end %>

  <%= link_to current_character.name_for(character), character_name_url(character.id) %>
</div>
