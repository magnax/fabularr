<section id="description">
  <div class="title-bar">
    <%= t('views.characters.attack.title') %>
  </div>
  <%= render partial: 'common/attack_form', locals: {
    target_id: target_id, target_type: 'character',
    weapons: weapons, force: force
  } %>
</section>