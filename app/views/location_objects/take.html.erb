You can take max. <%= @location_object.amount.to_i %> <%= @location_object.unit %> of <%= t("resources.#{@resource.key}") %>

<%= form_for :inventory_object, url: inventory_objects_path, method: 'POST' do |form| %>
  <%= form.hidden_field :subject_id, value: @location_object.subject_id %>
  <%= form.hidden_field :subject_type, value: @location_object.subject_type %>
  <%= form.text_field :amount %>
  <%= form.submit t('views.projects.start') %>
<% end %>